{
  "name": "gh-notifications",
  "version": "1.0.0",
  "description": "See your GitHub notifications at a glance — unread issues, PRs, reviews across all your repos",
  "bin": { "gh-notifications": "./cli.js" },
  "main": "./cli.js",
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.mjs" },
  "keywords": ["github", "notifications", "cli", "gh"],
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/sulthonzh/gh-notifications" },
  "engines": { "node": ">=18" }
}
